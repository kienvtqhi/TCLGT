<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kienvt.tclgt">

    <application
        android:name=".common.TclgtApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".example.MainActivity" />
        <activity android:name=".example.SecondActivity" />
        <activity android:name=".example.ThirdActivity" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />

        <meta-data
            android:name="AA_DB_NAME"
            android:value="tclgt.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="3" />
        <meta-data
            android:name="AA_MODELS"
            android:value="com.kienvt.tclgt.models.MOffence, com.kienvt.tclgt.models.MCategory" />

        <activity android:name=".example.DBActivity">

        </activity>
        <activity android:name=".example.AsyncTaskActivity">

        </activity>
    </application>

</manifest>
